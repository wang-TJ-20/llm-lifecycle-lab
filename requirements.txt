# Install from the repository root inside an activated Conda environment:
#   python -m pip install -r requirements.txt
#
# These are third-party runtime dependencies only. The scripts load the local
# src/ tree directly, so this file does not install llm-lifecycle-lab itself.
# Install a hardware-compatible PyTorch wheel first when using Linux CPU/CUDA.
PyYAML>=6.0,<7
numpy>=1.26,<3
torch>=2.4,<3
tokenizers==0.21.4
huggingface-hub==0.36.2
pyarrow==25.0.1
